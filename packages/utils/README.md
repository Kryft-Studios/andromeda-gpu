# @agpu/utils
@agpu/utils is a package built for utilities in andromeda-gpu. It provides high level abstractions like color, vector3, vector2, vector4, etc.

It also adds 3d helpers like `vertices`

## Installation
```bash
pnpm add ./@andromeda-3d/utils
```